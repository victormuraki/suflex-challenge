import styled from "styled-components";

export const FooterStyle = styled.footer`
    span{
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 1rem;
        font-size: 0.8rem;
        color: var(--text-body);
    }
`